# Lib Directory

This directory contains shared utilities and client configurations for the application.

## Structure
- Utility functions and classes that are shared across the application.
- Supabase client configuration.

## Module Boundaries
- Import only within `lib/` or from external libraries.
- Avoid circular dependencies.
